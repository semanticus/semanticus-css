# Panel

Similar to `.card`, but without the **shadow**. The `.panel` class creates a stylized container, ideal for grouping content in a visually distinct way.

Combined with `<ul>`, it can be used to create a list of items with a clean and organized appearance.

## Basic Usage

<HtmlPreviewer :code="PanelDemo.main()"/>

## List Group

<HtmlPreviewer :code="ListGroupDemo.main()"/>

<script setup>
import { PanelDemo } from "@demos/components";
import { ListGroupDemo } from "@demos/composites";
</script>
