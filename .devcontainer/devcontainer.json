{
  "name": "Playwright (Linux/Ubuntu 24.04)",
  "image": "mcr.microsoft.com/playwright:v1.59.1-noble",
  "postCreateCommand": "npm ci",
  "remoteUser": "pwuser",
  "initializeCommand": "if id -u pwuser >/dev/null 2>&1; then chown -R $(id -u pwuser):$(id -g pwuser) ${containerWorkspaceFolder}/node_modules || true; fi",
  "mounts": [
    "source=devcontainer_node_modules,target=${containerWorkspaceFolder}/node_modules,type=volume"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-playwright.playwright"
      ]
    }
  }
}
